Flask==3.0.3
requests==2.32.3
gunicorn==22.0.0

# AI-Mode. The OpenAI SDK is used against Ollama's OpenAI-compatible /v1
# endpoint, as set out in the course configuration guide.
openai==1.51.0

# httpx is pinned deliberately. openai 1.51.0 passes a `proxies` argument
# that httpx removed in 0.28, so an unpinned install builds an image whose
# every AI call dies with:
#     TypeError: Client.__init__() got an unexpected keyword argument 'proxies'
# It fails at client construction, so AI-Mode reports "unreachable" and the
# service quietly serves rule-based answers instead. Pinning the pair keeps
# the image reproducible.
httpx==0.27.2

python-dotenv==1.0.1
